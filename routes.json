[
    {
        "settings": [ "master" ],

        "default": {
            "path": "/use/:color",
            "call": "color.index"
        },

        "catch_all": {
            "path": "/",
            "call": "color.index"
        }
    },

    {
        "settings": [ "lang:de" ],

        "default": {
            "path": "/verwenden/:color"
        }
    }
]
